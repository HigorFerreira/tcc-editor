import styled from '@emotion/styled'

export const Container = styled("div")(() => ({
    margin: '12px 0'
}));

export const ImageTitle = styled("textarea")(() => ({
    padding: 5,
    outline: "none",
    border: "none",
    textAlign: "center",
    resize: "none",
    overflowY: "hidden",
    wordWrap: "break-word",
}));

export const Caption = styled("textarea")(() => ({
    padding: 5,
    outline: "none",
    border: "none",
    textAlign: "center",
    resize: "none",
    overflowY: "hidden",
    wordWrap: "break-word",
}));